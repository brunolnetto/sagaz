global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'sagaz'
    static_configs:
      - targets: ['host.docker.internal:8000'] # Assuming app runs locally
  
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']
